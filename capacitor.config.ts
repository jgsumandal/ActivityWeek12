import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ActivityWeek12',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
